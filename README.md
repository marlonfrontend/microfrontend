# microfrontend


## Build Setup

``` bash
# folder module starter
cd example

# folder module starter
cd root-config

# install dependencies
$ yarn install

# serve with hot reload at localhost:3000
$ yarn start

```
